// Dataflows — dynamic views for key scenarios.
// Every significant feature adds its own dynamic view here (or in feature-<name>.c4).

views {
  // Example — uncomment and adapt:
  //
  // dynamic view loginFlow {
  //   title 'Login Flow'
  //   user -> web 'opens /login'
  //   web -> api 'POST /auth/login'
  //   api -> db 'SELECT user'
  //   api -> web 'JWT'
  //   web -> user 'redirect /dashboard'
  //   autoLayout
  // }
}
